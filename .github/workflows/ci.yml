name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    uses: Platform-System/Platform.CI-CD/.github/workflows/build-dotnet.yml@main
    secrets: inherit
    with:
      project_path: 'Platform.SystemContext.csproj'
      dependencies: 'Platform-System/Platform.BuildingBlocks'




